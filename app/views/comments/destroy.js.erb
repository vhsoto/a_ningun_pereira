<% if @comment.destroyed?  %>
	$("#comment-" + <%= @comment.id %>).hide();
	$('.js-comments-count').html("<%= pluralize(@complaint.comments.count, 'Comentario')%>")
<% else %>
 	$("#comment-" + <%= @comment.id %>).prepend("<div class='alert alert-danger'><%= flash[:error] %></div>");
<% end %>